# implement_20260412_075941.md

작성 일시: `2026-04-12 07:59:41 KST`

이 문서는 현재 Coreline Orchestrator 프로젝트의 풀 테스트 범위를 고정하고, 검증이 일부 테스트만 실행하는 방향으로 축소되거나 목적 밖 점검으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적

현재 shipped 상태(v1 + post-v1 hardening + v2 + post-v2 + distributed production follow-up)를 기준으로 전체 프로젝트 검증 계획을 고정하고, 자동 검증/스모크/분산 경로까지 포함한 풀 테스트를 실행해 현재 상태를 재확인한다.

## 개발 범위

- 현재 문서/스크립트 기준의 전체 테스트 매트릭스 정리
- release hygiene / typecheck / full `bun test` / production build 실행
- v2 smoke, reattach smoke, migration dry-run, distributed smoke 실행
- 현재 테스트 범위와 미포함 수동 검증 항목 정리
- 결과를 기준으로 dev-plan 체크박스 업데이트

## 제외 범위

- 신규 기능 구현
- flaky test 리팩터링이나 테스트 코드 구조 개편
- manual provider-dependent real smoke 강제 실행
- 성능 벤치마크나 부하 테스트 추가

## 참조 문서

- [README](../README.md)
- [AGENTS.md](../AGENTS.md)
- [docs/OPERATIONS.md](../docs/OPERATIONS.md)
- [docs/V2-READINESS.md](../docs/V2-READINESS.md)
- [dev-plan/implement_20260411_225207.md](./implement_20260411_225207.md)

## 공통 진행 규칙

- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 다음으로 진행한다.
- 검증 명령은 현재 `package.json`의 shipped script를 우선 사용한다.
- 자동 검증에서 실패가 나면 해당 Phase에 이슈로 기록한다.
- manual real-worker smoke는 별도 외부 의존 검증으로 분리한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.

## Phase 상태 요약

- [x] Phase 1 완료 — 테스트 매트릭스 및 실행 기준 고정
- [x] Phase 2 완료 — 기본 자동 검증 실행
- [x] Phase 3 완료 — smoke / migration / distributed 검증 실행
- [x] Phase 4 완료 — 결과 요약 및 갭 정리

## Phase 1. 테스트 매트릭스 및 실행 기준 고정
### 목표
- 현재 프로젝트에서 무엇을 풀 테스트로 볼지 명시적으로 고정한다.

### 구현 태스크
- [x] `README.md`, `AGENTS.md`, `docs/OPERATIONS.md`, 최신 dev-plan을 읽고 현재 테스트 범위를 고정한다.
- [x] `package.json` scripts 기준 기본 검증 명령과 smoke 검증 명령을 분리해 정의한다.
- [x] manual real smoke를 이번 자동 검증 범위 밖으로 명시한다.

### 자체 테스트
- [x] 테스트 매트릭스가 현재 문서 및 scripts와 충돌하지 않는다.

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 2. 기본 자동 검증 실행
### 목표
- 저장소 전체 기준 release/build/test/typecheck 상태를 재확인한다.

### 구현 태스크
- [x] `bun run check:release-hygiene` 실행
- [x] `bunx tsc --noEmit` 실행
- [x] `bun test` 실행
- [x] `bun run build` 실행

### 자체 테스트
- [x] 위 4개 명령이 모두 성공한다.

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 3. Smoke / Migration / Distributed 검증 실행
### 목표
- shipped ops 경로와 분산 경로까지 포함한 통합 검증을 다시 실행한다.

### 구현 태스크
- [x] `bun run ops:verify:v2` 실행
- [x] `bun run ops:migrate:dry-run` 단독 실행 확인
- [x] `bun run ops:smoke:session:reattach:fixture` 실행
- [x] `bun run ops:verify:distributed` 실행
- [x] `command -v codexcode && codexcode --help` preflight 실행

### 자체 테스트
- [x] v2 smoke/migration 번들이 종료까지 성공한다.
- [x] same-session reattach fixture smoke가 종료까지 성공한다.
- [x] distributed prototype/service smoke 번들이 성공한다.

### 이슈 및 수정
- [x] `bun run ops:verify:v2` hang 원인은 reattach smoke / migration dry-run CLI가 결과 출력 후 Bun 프로세스를 종료하지 못하던 점으로 확인했다.
- [x] `bun run ops:migrate:dry-run`는 script 종료 시 stdout flush 후 `process.exit(0)`로 수렴시켜 정상 종료를 확인했다.
- [x] `bun run ops:smoke:session:reattach:fixture`도 script 종료 시 stdout flush 후 `process.exit(0)`로 수렴시켜 정상 종료를 확인했다.
- [x] `bun run ops:verify:distributed`는 정상 통과한다.

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 4. 결과 요약 및 갭 정리
### 목표
- 자동 검증 결과를 현재 상태표와 함께 남기고 수동 잔여 검증을 분리한다.

### 구현 태스크
- [x] 통과/실패 결과와 핵심 수치를 정리한다.
- [x] 자동 범위와 수동 범위를 구분해 남은 테스트 갭을 정리한다.
- [x] 현재 작업트리 상태와 해석을 함께 기록한다.

### 자체 테스트
- [x] 결과 요약이 실제 실행 명령과 일치한다.

### 이슈 및 수정
- [x] 종료 hang 이슈 수정 후 `ops:verify:v2` 재검증까지 완료했다.

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 전체 테스트 workstream 종료 가능

## 최종 결과 요약

### 성공한 자동 검증
- `bun run check:release-hygiene` ✅
- `bunx tsc --noEmit` ✅
- `bun test` ✅ (`215 pass / 0 fail`)
- `bun run build` ✅
- `bun run ops:smoke:fixture` ✅
- `bun run ops:smoke:timeout:fixture` ✅
- `bun run ops:smoke:v2:session:fixture` ✅
- `bun run ops:verify:distributed` ✅
- `bun run ops:smoke:session:reattach:fixture` ✅
- `bun run ops:migrate:dry-run` ✅
- `bun run ops:verify:v2` ✅
- `command -v codexcode && codexcode --help` ✅

### 조사 결과 및 수정
- `verifySessionReattach=true` 경로에서 CLI script는 결과 출력 후에도 Bun 프로세스가 종료되지 않았다.
- 조사 시점 기준 `process.getActiveResourcesInfo()` / `_getActiveHandles()`는 비어 있었지만, 프로세스 레벨에서는 종료가 지연됐다.
- 실사용 영향은 `run-ops-smoke.ts`, `run-v2-migration-dry-run.ts` 같은 CLI 진입점에 국한되므로:
  - stdout flush
  - `process.exit(0)`
  로 종료를 명시해 `ops:smoke:session:reattach:fixture`, `ops:migrate:dry-run`, `ops:verify:v2`를 green 상태로 복구했다.

### 현재 작업트리 해석
- 저장소는 `main...origin/main [ahead 1]` 상태다.
- 이번 턴 변경에는 `dev-plan/implement_20260412_075941.md`, `src/ops/smoke.ts`, `scripts/run-ops-smoke.ts`, `scripts/run-v2-migration-dry-run.ts`가 포함된다.
- 나머지 대규모 변경은 기존 distributed follow-up 작업의 미커밋 상태다.

## 잔여 리스크 / 후속 과제

- manual real-worker smoke는 provider 인증/실환경 의존성이 있어 자동 번들 밖에 남았지만, 2026-04-12 operator-machine 실행 기록은 `docs/REAL-SMOKE-REPORT-20260412.md`로 고정했다.
- 성능/장기 soak/fault-injection 테스트는 현재 shipped smoke 범위에 포함되지 않지만, 최소 harness와 weekly cadence는 `docs/DEEP-VERIFICATION.md` 및 `bun run ops:verify:deep:weekly`로 고정했다.
- Bun 런타임 레벨의 종료 지연 근본 원인은 추가 관찰 여지가 있지만, probe evidence와 issue draft는 `docs/BUN-EXIT-PROBE.md` / `docs/BUN-EXIT-ISSUE-DRAFT-20260412.md`로 축적했고 shipped CLI 검증 경로는 정상 종료를 유지한다.


## 후속 closure 연결

- manual real-worker smoke follow-up: `docs/REAL-SMOKE-REPORT-20260412.md`
- deep verification cadence: `docs/DEEP-VERIFICATION.md`, `bun run ops:verify:deep:weekly`
- Bun exit-delay issue/probe accumulation: `docs/BUN-EXIT-PROBE.md`, `docs/BUN-EXIT-ISSUE-DRAFT-20260412.md`
